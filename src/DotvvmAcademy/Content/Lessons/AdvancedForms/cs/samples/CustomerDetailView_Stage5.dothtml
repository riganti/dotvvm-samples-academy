<p>
    <dot:TextBox Text="{value: FirstName}" />
    <dot:TextBox Text="{value: LastName}" />
</p>
<p>
    Role:
    <dot:RadioButton CheckedItem="{value: Role}"
                     CheckedValue="A"
                     Text="Admin" />
    <dot:RadioButton CheckedItem="{value: Role}"
                     CheckedValue="U"
                     Text="User" />
</p>
<p>
    <!-- pøidejte první CheckBox zde -->
    <!-- pøidejte druhý CheckBox zde -->
    <!-- pøidejte tøetí CheckBox zde -->
</p>